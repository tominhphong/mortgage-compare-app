# Cloudflare Worker API base URL for scenario cloud sync (Sprint 4 optional feature).
# Leave empty to disable cloud save/load; localStorage remains the source of truth.
# After running `wrangler deploy` in ../worker, set to e.g.:
#   https://mortgage-compare-api.YOUR_SUBDOMAIN.workers.dev
NEXT_PUBLIC_API_BASE=
